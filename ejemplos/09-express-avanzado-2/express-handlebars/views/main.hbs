<h1>Lista de los campeones mas sugeridos</h1>

{{#if listExists}}
    <ul>
        {{#each suggestedChamps}}
            <li class={{this.lane}}>{{this.name}}</li>
        {{/each}}
    </ul>
{{/if}}
